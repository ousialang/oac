package ousia.lambda;


# Provides common terms and other utilities for the untyped Lambda calculus.
#
# @author Filippo Costa
object Lambda:

  val true = λ[a,b => a];
  val false = λ[a,b => b];

  val not = λ[b => b[false, true]];
  val or = λ[x,y => x[true, y]];
  val and = λ[x,y => x[y, false]];
  val xor = λ[x,y => x[not y][y]];

  while (x > 2) do:
    x++;
    Console println (Double Sin Pi/2);

  if Or[true,false] then:
    val foo: "bar";
  else
    val bar: "foo";

