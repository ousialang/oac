// Provides common terms and other utilities for the untyped Lambda calculus.
ns LAMBDA {

  let true = a => b => a
  let false = a => b => b

  let not = b => b(false, true)
  let or = (x,y) => x(true, y)
  let and = (x,y) => x(y, false)
  let xor = (x,y) => x(not y)(y)

  while (x > 2) {
    x++
    println (Pi/2.sin to Double) }

  if (true or false) then {
    let foo = "bar" }
  else {
    let bar = "foo" }}
