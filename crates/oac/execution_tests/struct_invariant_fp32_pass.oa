struct Foo {
	x: FP32,
}

invariant "x is bounded above by one" for (v: Foo) {
	return v.x <= 1.0
}

fun make_foo(x: FP32) -> Foo {
	return Foo struct { x: x, }
}

fun main() -> I32 {
	f = make_foo(0.5)
	if f.x < 1.0 {
		print(1)
	}
	return 0
}
