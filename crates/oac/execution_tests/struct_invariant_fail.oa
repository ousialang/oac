struct Counter {
	value: I32,
}

invariant positive_value "counter value must be non-negative" for (v: Counter) {
	return v.value >= 0
}

fun make_counter() -> Counter {
	return Counter struct { value: sub(0, 1), }
}

fun main() -> I32 {
	c = make_counter()
	print(c.value)
	return 0
}
