object Example_Application {

  System println (1,2,3) map (x => x+1) where (x => x>2) to [String];
}

// Maps
(1: 2, 2: 3, "something": "that");

2 in map; // true
map contains 2; // true

(1,2,3,4,5) map (x => x*2)

// range
1 to 10 by 2
10 to 1 by -2

if (condition) then {
  do_something();
} else {
  do_something_else();
}

while (condition) do {
  System.write "hello"
}

do {
  "hello" print
} while (condition);

2 √ 9; // 3
