#include <cstdio>
#include <sysexits.h>

int help_main(int argc, char* argv[]) {
	system("man " + )
	std::cout << "Help not available" << std::endl;
	char* zenzi_dir = main_zenzi_dir();
	return EX_OK;
}

int help_404_command() {
	std::cout << "Error: '" << cmd << "' is not a valid command.";

}

int help_most_similar_command(std::vector<string>) {

}
